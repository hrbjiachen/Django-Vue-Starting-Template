# Django Vue Starting Template

## Installation

- clone the project
- Setup the frontend

```cmd
cd frontend
npm install
npm run build

```

- Start the server(root directory)

```cmd
python manage.py runserver
```
